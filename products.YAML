version 3.8
services:
 mysql-container:
  image: mysql:latest
  environment:
   MYSQL_ROOT_PASSWORD: admin
  ports:
   3306:3306
  volumes:
   ./mysql_init:docker-entrypoint=initds,
 products-microservice: